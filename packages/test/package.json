{
  "name": "@typesafe-sql/test",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "env $(cat .env | xargs) psql -f testdata.sql",
    "test": "jest"
  },
  "devDependencies": {
    "dotenv": "^14.3.2",
    "@typesafe-sql/describe-query": "^0.1.0",
    "pg": "~8.7.1"
  }
}
