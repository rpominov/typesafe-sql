{
  "name": "@typesafe-sql/test",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "env $(cat .env | xargs) psql -f testdata.sql && jest"
  },
  "dependencies": {
    "@typesafe-sql/describe-query": "^0.1.0",
    "pg": "~8.7.1"
  },
  "devDependencies": {
    "dotenv": "^14.3.2"
  }
}
