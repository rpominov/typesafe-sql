{
  "name": "tmp-test-project",
  "private": true,
  "scripts": {
    "build-json": "typesafe-pg build",
    "build-res": "typesafe-pg build --config ./rescript-config.json",
    "build:ci": "npm run build-json && npm run build-res"
  },
  "dependencies": {
    "@typesafe-sql/rescript-pg": "^0.1.0",
    "@typesafe-sql/pg-cli": "^0.1.0",
    "@typesafe-sql/generator-pg-rescript": "^0.1.0",
    "rescript": "^9.1.4"
  }
}
